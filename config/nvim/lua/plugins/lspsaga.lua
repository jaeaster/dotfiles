require('lspsaga').setup {
  lightbulb = {
    enable = false, -- Disable the lightbulb completely
    enable_in_insert = false,
    sign = false,
    sign_priority = 40,
    virtual_text = false,
  },
}
